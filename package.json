{
  "name": "test",
  "private": true,
  "scripts": {
    "start": "node -r dotenv/config env.js && meteor run"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "dotenv": "^4.0.0",
    "fine-uploader": "^5.14.2",
    "meteor-node-stubs": "~0.2.4"
  }
}
